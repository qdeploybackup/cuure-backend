To run this backend server 
    To install all the dependencies : npm install
    To run the code : npm start
The port will be active in 3000
    For the admin page : http://localhost:3000/admin

For the database access the database is stored in Postgre SQL and is presently in production in railway(https://railway.com/)

To enter into your database 
    \c your_database_name